.idea/
build/
vendor/

# Configuration
*.toml
*.json
config.yaml
# Allow repo-default Callisto config (other `config.yaml` paths stay ignored)
!.callisto/config.yaml

# Coverage
coverage.*

# Local indexer logs (dev-stack.sh start-bg)
logs/
callisto-sync.log
