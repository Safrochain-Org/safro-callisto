.idea/
build/
vendor/

# Configuration
*.toml
*.json
config.yaml

# Callisto home: copy `.callisto/config.yaml.example` → `.callisto/config.yaml` locally
.callisto/config.yaml

# Coverage
coverage.*

# Local indexer logs (dev-stack.sh start-bg)
logs/
callisto-sync.log
